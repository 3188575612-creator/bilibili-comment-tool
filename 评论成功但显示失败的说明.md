# 评论成功但显示失败的问题说明

## 问题描述

用户反馈：评论实际上发送成功了（在B站上可以看到评论），但程序显示：
```
[失败] 评论发送失败
   错误码: -1
   错误信息: 未知错误
```

## 原因分析

B站的 `send_comment` API 在成功时可能返回以下几种格式：

1. **标准成功**：`{"code": 0, "message": "0", "data": {...}}`
2. **返回 None**：表示成功
3. **非标准成功**：`{"code": -1, "message": "...", "data": {"rpid": ...}}`

第三种情况是问题所在：虽然评论已成功发送，但返回的 `code` 是 `-1` 而不是 `0`。

## 解决方案

修改了判断逻辑，现在会检查：

1. **code == 0**：标准成功
2. **有 data 字段且包含 rpid（评论ID）**：说明评论已成功发送
3. **code == -1 但有 data 字段**：非标准成功返回
4. **其他情况**：真正的失败

## 判断逻辑

```python
if result is None:
    # 返回None表示成功
    success = True
elif isinstance(result, dict):
    code = result.get('code', -1)
    data = result.get('data', {})
    
    if code == 0:
        # 标准成功
        success = True
    elif data and isinstance(data, dict) and 'rpid' in data:
        # 有评论ID说明成功
        success = True
    elif code == -1 and data:
        # code=-1 但有数据，可能是成功的非标准返回
        success = True
    else:
        # 真正的失败
        success = False
else:
    # 非字典返回值，可能是成功
    success = True
```

## 更新的文件

- `main_v2.py` - 主程序（v2版本）
- `main.py` - 主程序（v1版本）
- `test_comment.py` - 测试脚本

## 验证方法

1. 运行程序发送评论
2. 检查B站上是否能看到评论
3. 查看程序输出是否显示"成功"

## 注意事项

- 如果B站上能看到评论，说明评论已成功发送
- 程序现在会正确识别非标准的成功返回
- 评论历史会正确记录已发送的评论

## 技术细节

B站API的返回格式可能因版本更新而变化。程序现在使用更宽容的判断逻辑：
- 检查是否有评论ID（rpid）
- 检查是否有数据字段
- 而不是仅仅依赖 code 字段

这种改进使得程序能够处理B站API的各种返回格式，提高兼容性。
