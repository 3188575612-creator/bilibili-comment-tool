# B站视频搜索和评论工具 - 项目结构

## 项目目录结构

```
bilibili_comment_tool/
├── main.py              # 主程序（v1版本）
├── main_v2.py           # 主程序（v2版本，推荐使用）
├── test_search.py       # 搜索功能测试脚本（v1版本）
├── test_search_v2.py    # 搜索功能测试脚本（v2版本）
├── requirements.txt     # Python依赖包列表
├── config.example.json  # 配置文件示例
├── config.json          # 配置文件（程序自动生成）
├── README.md            # 项目使用说明
├── 快速开始.md          # 快速开始指南
├── 安装说明.md          # 详细安装说明
├── 项目总结.md          # 项目技术总结
└── 项目结构.md          # 本文档
```

## 文件说明

### 核心程序文件

#### `main.py` (v1版本)
- **功能**：B站视频搜索和评论工具的主程序
- **特点**：完整的搜索和评论功能
- **适用**：Linux/macOS系统或支持UTF-8编码的Windows终端

#### `main_v2.py` (v2版本，推荐)
- **功能**：B站视频搜索和评论工具的主程序（改进版）
- **特点**：
  - 支持Windows控制台中文显示
  - 解决了编码问题
  - 更好的用户体验
- **适用**：所有操作系统，特别是Windows

### 测试文件

#### `test_search.py`
- **功能**：测试搜索功能（v1版本）
- **用途**：验证搜索功能是否正常工作
- **运行**：`python test_search.py`

#### `test_search_v2.py`
- **功能**：测试搜索功能（v2版本）
- **用途**：验证搜索功能是否正常工作（支持中文显示）
- **运行**：`python test_search_v2.py`

### 配置文件

#### `requirements.txt`
- **内容**：Python依赖包列表
- **用途**：安装项目所需的依赖包
- **使用**：`pip install -r requirements.txt`

#### `config.example.json`
- **内容**：配置文件示例
- **用途**：展示配置文件的格式
- **说明**：实际配置文件由程序自动生成

#### `config.json`
- **内容**：用户配置文件
- **用途**：存储用户的B站登录凭证
- **生成**：程序首次运行时自动生成
- **格式**：
  ```json
  {
    "sessdata": "用户的SESSDATA值",
    "bili_jct": "用户的bili_jct值",
    "buvid3": "用户的buvid3值"
  }
  ```

### 文档文件

#### `README.md`
- **内容**：项目使用说明
- **包含**：
  - 项目介绍
  - 功能特点
  - 安装说明
  - 使用方法
  - 注意事项
  - 技术说明

#### `快速开始.md`
- **内容**：快速开始指南
- **包含**：
  - 安装步骤
  - 获取登录凭证
  - 运行程序
  - 使用流程
  - 常见问题

#### `安装说明.md`
- **内容**：详细安装说明
- **包含**：
  - 系统要求
  - 安装步骤
  - 获取登录凭证
  - 运行程序
  - 常见安装问题
  - 更新依赖

#### `项目总结.md`
- **内容**：项目技术总结
- **包含**：
  - 项目概述
  - 技术实现
  - 使用流程
  - 注意事项
  - 测试结果
  - 后续优化方向

## 代码结构说明

### 主要类和方法

#### `BilibiliVideo` 类
- **功能**：B站视频数据类
- **属性**：
  - `bvid`：视频BV号
  - `title`：视频标题
  - `author`：UP主名称
  - `play_count`：播放量
  - `danmaku_count`：弹幕数量
  - `duration`：视频时长
  - `description`：视频简介
  - `url`：视频链接

#### `BilibiliCommentTool` 类
- **功能**：B站视频搜索和评论工具主类
- **主要方法**：
  - `load_config()`：加载配置文件
  - `save_config()`：保存配置文件
  - `setup_credential()`：设置登录凭证
  - `search_videos()`：搜索B站视频
  - `display_videos()`：显示搜索结果
  - `send_comment()`：发送评论
  - `batch_comment()`：批量发送评论
  - `get_user_selection()`：获取用户选择
  - `run()`：运行主程序
  - `search_and_comment()`：搜索视频并评论的完整流程

### 依赖关系

```
main.py / main_v2.py
├── bilibili_api.search      # 搜索模块
├── bilibili_api.comment     # 评论模块
├── bilibili_api.Credential  # 认证模块
├── bilibili_api.video       # 视频模块
└── aiohttp                  # 异步HTTP请求
```

## 使用建议

### 选择版本
- **Windows用户**：推荐使用 `main_v2.py`
- **Linux/macOS用户**：可以使用 `main.py` 或 `main_v2.py`

### 测试功能
1. 先运行 `test_search_v2.py` 测试搜索功能
2. 确认搜索功能正常后，再运行主程序

### 配置管理
- 登录凭证会自动保存到 `config.json`
- 如需更换账号，可在程序中选择"重新设置登录凭证"
- 妥善保管 `config.json` 文件，不要泄露给他人

## 开发环境

- **Python版本**：3.13.12
- **操作系统**：Windows 10/11
- **开发工具**：VS Code / PyCharm
- **依赖管理**：pip

## 版本历史

### v1.0 (2026-06-02)
- 实现基本搜索和评论功能
- 支持批量评论
- 实现配置管理

### v2.0 (2026-06-02)
- 解决Windows控制台中文显示问题
- 优化用户界面
- 改进错误处理

## 许可证

MIT License