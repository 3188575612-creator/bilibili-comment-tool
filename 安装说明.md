# B站视频搜索和评论工具 - 安装说明

## 系统要求

- Python 3.7 或更高版本
- Windows/Linux/macOS 操作系统
- 稳定的网络连接

## 安装步骤

### 1. 安装Python（如果尚未安装）

访问 https://www.python.org/downloads/ 下载并安装Python。

### 2. 安装依赖包

打开命令行（终端），进入项目目录，运行以下命令：

```bash
pip install -r requirements.txt
```

或者手动安装：

```bash
pip install bilibili-api-python aiohttp
```

### 3. 验证安装

运行以下命令验证安装是否成功：

```bash
python -c "import bilibili_api; print('安装成功')"
```

如果输出"安装成功"，说明依赖包已正确安装。

## 获取B站登录凭证

使用此工具需要B站的登录凭证，请按以下步骤获取：

### 方法一：从浏览器Cookie获取

1. 打开浏览器，访问 https://www.bilibili.com/ 并登录
2. 按 `F12` 打开开发者工具
3. 切换到 `Application`（Chrome）或 `存储`（Firefox）标签
4. 在左侧找到 `Cookies` -> `https://www.bilibili.com`
5. 找到并复制以下Cookie值：
   - `SESSDATA`
   - `bili_jct`
   - `buvid3`（可选）

### 方法二：使用浏览器扩展

1. 安装Cookie查看扩展（如 EditThisCookie）
2. 访问B站并登录
3. 使用扩展查看并复制Cookie值

## 运行程序

### 使用v2版本（推荐）

```bash
python main_v2.py
```

### 使用v1版本

```bash
python main.py
```

## 常见安装问题

### Q: pip命令未找到
A: 尝试使用 `python -m pip install -r requirements.txt`

### Q: 权限不足
A: 在Windows上以管理员身份运行命令提示符；在Linux/macOS上使用 `sudo`

### Q: 网络连接问题
A: 尝试使用国内镜像源：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: Python版本不兼容
A: 请确保使用Python 3.7或更高版本。可以通过以下命令检查版本：
```bash
python --version
```

## 更新依赖

如果需要更新到最新版本：

```bash
pip install --upgrade bilibili-api-python aiohttp
```

## 卸载

如果需要卸载：

```bash
pip uninstall bilibili-api-python aiohttp
```

## 技术支持

如遇到安装问题，请检查：
1. Python版本是否符合要求
2. 网络连接是否正常
3. 是否有足够的权限安装软件包

更多帮助请参考：
- bilibili-api-python 官方文档：https://github.com/Nemo2011/bilibili-api
- Python 官方文档：https://docs.python.org/