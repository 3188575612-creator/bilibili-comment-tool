# 评论发送失败排查指南

## 快速排查步骤

### 1. 运行测试脚本
```bash
python test_comment.py
```
这个脚本会：
- 检查登录凭证
- 测试获取视频信息
- 尝试发送评论
- 显示详细的错误信息

### 2. 检查常见错误

#### 错误码 -101：未登录或登录过期
**症状**：提示"未登录"或"登录凭证无效"
**解决**：
1. 重新登录B站
2. 按F12打开开发者工具
3. 切换到 Application -> Cookies
4. 重新复制 SESSDATA 和 bili_jct
5. 运行主程序，选择"重新设置登录凭证"

#### 错误码 -412：请求过于频繁
**症状**：提示"请求被拦截"或"风控"
**解决**：
1. 等待10-30分钟后再试
2. 增加评论发送间隔（建议10秒以上）
3. 减少批量评论的数量
4. 使用代理IP

#### 错误码 12002：评论区已关闭
**症状**：提示"评论区已关闭"
**解决**：
- 该视频的UP主关闭了评论功能
- 换一个视频测试

#### 错误码 12009：评论内容被过滤
**症状**：提示"评论内容包含敏感词"
**解决**：
- 修改评论内容，避免特殊字符
- 避免使用可能被过滤的词汇

#### 错误码 -403：权限不足
**症状**：提示"权限不足"或"被限制"
**解决**：
- 账号可能被B站限制评论
- 等待24小时后再试
- 使用其他账号测试

### 3. 检查网络连接
```bash
# 测试网络连接
ping www.bilibili.com

# 测试API连接
curl -I https://api.bilibili.com
```

### 4. 检查视频是否支持评论
1. 在浏览器中打开视频页面
2. 查看评论区是否存在
3. 尝试手动发送评论

## 详细排查流程

### 第一步：验证登录凭证
```bash
python -c "
import json
with open('config.json', 'r') as f:
    config = json.load(f)
print('SESSDATA长度:', len(config.get('sessdata', '')))
print('bili_jct长度:', len(config.get('bili_jct', '')))
"
```

正常值：
- SESSDATA：通常100-300字符
- bili_jct：通常20-30字符

### 第二步：测试视频信息获取
```bash
python -c "
import asyncio
from bilibili_api import video, Credential
import json

async def test():
    with open('config.json') as f:
        config = json.load(f)
    credential = Credential(sessdata=config['sessdata'], bili_jct=config['bili_jct'])
    v = video.Video(bvid='BV1GJ411x7h7', credential=credential)
    info = await v.get_info()
    print('视频标题:', info['title'])
    print('AID:', info['aid'])

asyncio.run(test())
"
```

### 第三步：测试评论功能
```bash
python test_comment.py
```

## 常见问题解答

### Q1: 为什么之前能评论，现在不行了？
A: 可能原因：
- 登录凭证过期（通常1个月左右）
- 账号被临时限制评论
- B站更新了API或风控策略

### Q2: 如何避免被风控？
A: 建议：
- 增加评论发送间隔（建议10秒以上）
- 不要在短时间内发送大量评论
- 避免发送重复内容
- 使用不同的评论内容

### Q3: 评论内容有什么限制？
A: 注意事项：
- 避免特殊字符和表情符号
- 避免可能被过滤的敏感词
- 评论长度不要过长
- 不要发送纯数字或无意义内容

### Q4: 如何更新登录凭证？
A: 步骤：
1. 重新登录B站
2. 按F12打开开发者工具
3. 切换到 Application -> Cookies
4. 找到并复制 SESSDATA 和 bili_jct
5. 运行主程序，选择"重新设置登录凭证"
6. 输入新的凭证

## 获取帮助

如果以上方法都无法解决问题，请：
1. 运行 `python test_comment.py` 获取详细错误信息
2. 记录错误码和错误信息
3. 查看B站官方API文档或社区讨论

## 预防措施

1. **定期更新凭证**：每个月更新一次登录凭证
2. **控制发送频率**：每次发送间隔10秒以上
3. **分散发送时间**：不要集中在短时间内发送
4. **使用代理**：如果频繁被风控，考虑使用代理IP
5. **备份配置**：定期备份 config.json 文件
